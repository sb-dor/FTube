import 'package:flutter/material.dart';

class VideoInformationErrorWidget extends StatelessWidget {
  const VideoInformationErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
