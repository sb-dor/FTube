import 'package:flutter/material.dart';

class LoadingSearchBodyScreen extends StatelessWidget {
  const LoadingSearchBodyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
